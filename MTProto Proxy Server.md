MTProto Proxy - это специальный сервер, который позволяет обходить блокировки Telegram, обеспечивая быструю и безопасную связь с серверами Telegram даже в условиях цензуры интернета.

📋 Описание
MTProto Proxy работает как посредник между клиентом Telegram и серверами Telegram. Он использует протокол MTProto, который разработан специально для Telegram, обеспечивая высокую производительность и безопасность.

✨ Особенности
Обход блокировок провайдеров
Высокая скорость соединения
Совместимость с официальными клиентами Telegram
Защита от DPI-анализа
Поддержка различных методов маскировки
🚀 Установка
Требования
VPS или сервер с доступом по SSH
Операционная система: Debian, Ubuntu или CentOS
Docker установлен на сервере
Вариант 1: Официальный прокси (рекомендуется)
Проще, стабильнее, не требует кастомных конфигов. Подходит для 95% случаев.

Шаг 1: Установите Docker (если ещё не установлен)
# Для Debian/Ubuntu
curl -fsSL https://get.docker.com | sh

# Для CentOS/RHEL
curl -fsSL https://get.docker.com | sh && systemctl enable --now docker
Шаг 2: Сгенерируйте случайный секрет (32 hex-символа)
openssl rand -hex 16
# Пример вывода: a3f8c1d9e7b245068912ab34cd56ef78
Скопируйте полученный секрет — он понадобится в шаге 3 и для ссылки в Telegram.

Шаг 3: Запустите прокси
docker run -d \
  --name mtproto-proxy \
  --restart=unless-stopped \
  -p 443:443 \
  -e SECRET=$(openssl rand -hex 16) \
  telegrammessenger/proxy:latest
💡 Важно: Официальный прокси работает ТОЛЬКО на порту 443. Если порт занят (например, AmneziaVPN), используйте Вариант 2 ниже.

Шаг 4: Получите ссылку для подключения
docker logs mtproto-proxy 2>&1 | grep -oP 'tg://proxy\?server=[^ ]+'
Пример вывода:

tg://proxy?server=185.170.123.45&port=443&secret=b7e8c1d9e7b245068912ab34cd56ef12
Скопируйте эту ссылку → вставьте в Telegram → нажмите «Подключиться».

Вариант 2: Прокси с переадресацией портов (когда порт 443 занят)
Используйте этот вариант, если порт 443 занят другим сервисом (например, Amnezia VPN).

Шаг 1: Удалите старые контейнеры (если были)
docker rm -f mtproto-proxy
Шаг 2: Сгенерируйте секрет
SECRET=$(head -c 16 /dev/urandom | xxd -ps)
echo "Ваш секрет: $SECRET"
Шаг 3: Запустите прокси на порту 8443
docker run -d \
  --name mtproto-proxy \
  --restart=unless-stopped \
  -p 8443:443 \
  -e SECRET=$SECRET \
  -e WORKERS=1 \
  telegrammessenger/proxy:latest
Шаг 4: Откройте порт в firewall
# Для iptables
iptables -I INPUT -p tcp --dport 8443 -j ACCEPT

# Для UFW
ufw allow 8443/tcp
Шаг 5: Получите правильную ссылку
# Получаем IP и Секрет
PUBLIC_IP=$(curl -s https://api.ipify.org)
MY_SECRET=$(docker exec mtproto-proxy cat /data/proxy-secret)

echo ""
echo "========================================="
echo "✅ ВАША ССЫЛКА (С защитой от блокировок):"
echo "========================================="
echo "tg://proxy?server=${PUBLIC_IP}&port=8443&secret=dd${MY_SECRET}"
echo "========================================="
Вариант 3: Прокси с FakeTLS (маскировка под HTTPS)
Нужен если:

Порт 443 занят другим сервисом (например, AmneziaVPN)
Требуется обход более строгих блокировок через маскировку под легитимный трафик
Шаг 1: Создайте конфигурационный файл
mkdir -p ~/mtproto-proxy && cd ~/mtproto-proxy

cat > config.py << 'EOF'
PORT = 443
USERS = {
    "user1": "b7e8c1d9e7b245068912ab34cd56ef12"  # Замените на свой секрет (32 hex символа)
}
TLS_DOMAIN = "www.google.com"  # Обязательно с www.
EOF
⚠️ Важно: Секрет должен быть ровно 32 шестнадцатеричных символа (цифры 0-9, буквы a-f). Не используйте префикс ee в конфиге!

Шаг 2: Запустите прокси на свободном порту (например, 8443)
docker run -d \
  --name mtproto-proxy \
  --restart=unless-stopped \
  -p 8443:443 \
  -v ~/mtproto-proxy/config.py:/home/tgproxy/config.py:ro \
  alexbers/mtprotoproxy
Шаг 3: Сформируйте ссылку для Telegram вручную
tg://proxy?server=ВАШ_IP&port=8443&secret=eeВАШ_СЕКРЕТ_32_СИМВОЛА7777772e676f6f676c652e636f6d
Где:

ee — префикс для включения FakeTLS
ВАШ_СЕКРЕТ_32_СИМВОЛА — ваш секрет из config.py (без изменений)
7777772e676f6f676c652e636f6d — hex-код домена www.google.com (фиксированная часть)
🔍 Диагностика проблем
Симптом	Причина	Решение
Bad secret в логах	Секрет ≠ 32 символа или содержит ee	Используйте ровно 32 hex-символа БЕЗ префикса
Порт не отвечает	Фаервол/провайдер блокирует порт	Откройте порт: ufw allow 443/tcp или используйте 8443
Статус «Недоступен» в Telegram	Неправильный формат ссылки	Для официального прокси — без ee; для FakeTLS — с ee + hex-домен
Контейнер падает сразу	Порт 443 занят другим сервисом	Используйте Вариант 2 с портом 8443
✅ Рекомендации
Начните с Варианта 1 — он проще и надёжнее.
Используйте Вариант 2 только если:
Порт 443 занят
Нужна маскировка под HTTPS (редко требуется в 2026 году)
Никогда не передавайте секрет с префиксом ee в конфигурацию сервера — только в ссылку для клиента.
🛡 Безопасность
Используйте уникальные секреты для каждого прокси
Регулярно обновляйте Docker-образы
Мониторьте логи на предмет аномальной активности
Ограничьте доступ к портам через фаервол
🤝 Вклад в проект
Если вы нашли ошибку или хотите улучшить документацию, пожалуйста, создайте Issue или Pull Request.

📞 Поддержка
Если у вас возникли проблемы с настройкой, проверьте диагностику выше или обратитесь за помощью к сообществу.
